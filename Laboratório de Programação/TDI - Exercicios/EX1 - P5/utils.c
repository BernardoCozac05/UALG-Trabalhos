#include "utils.h"
#include <math.h>
// implementação de um serviço muito simples
int add(int a, int b) {
    return a + b;
}

// Serviço de Multiplicação:

int multiply(int a, int b) {
    return a * b;
}

int powerOf(int a, int b) {
    return (int)(pow(a,b));
}